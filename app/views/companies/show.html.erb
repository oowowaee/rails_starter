<h1><%= t('company.edit') %></h1>

<%= render 'shared/errors' %>

<%= form_for :company, url: company_path(@company), method: :patch do |f| %>
  <%= render partial: 'form', locals: { f: f } %>
<% end %>